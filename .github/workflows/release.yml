name: Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
      - '[0-9]+.[0-9]+.[0-9]+'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'

      - name: Install gettext
        run: sudo apt-get install -y gettext

      - name: Compile gettext .po files (app)
        run: find locale -name "*.po" -exec sh -c 'msgfmt "$1" -o "${1%.po}.mo"' _ {} \;

      - name: Compile gettext .po files (default theme)
        run: find themes/default/locale -name "*.po" -exec sh -c 'msgfmt "$1" -o "${1%.po}.mo"' _ {} \;

      - name: Stamp css/backend.css with release version
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          sed -i "1i /* version: ${VERSION}, (c)Syrnik.com */" css/backend.css

      - name: Run compress-app.php
        run: php compress-app.php auth -app-path .

      - name: Get tag message
        id: tag_message
        run: |
          git fetch --tags --force
          MESSAGE=$(git tag -l --format='%(contents)' ${{ github.ref_name }})
          echo "message<<EOF" >> $GITHUB_OUTPUT
          echo "$MESSAGE" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          body: ${{ steps.tag_message.outputs.message }}
          files: |
            *.zip
            *.tar.gz
