<?xml version="1.0"?>
<psalm
    errorLevel="4"
    phpVersion="8.5"
    resolveFromConfigFile="true"
    cacheDirectory="tests/.psalm-cache-85"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
    autoloader="tests/psalm-init.php"
    errorBaseline="psalm85-baseline.xml"
>
    <projectFiles>
        <directory name="lib" />
        <!-- Scanned for symbol resolution (waContact, waModel, wa()...) but not
             reported on: sdekint's own extraFiles do get analyzed by this Psalm
             version, which floods the report with framework issues nobody here
             can fix. ignoreFiles keeps the scan without the noise. wa-apps/ is
             deliberately not listed: wa-apps/auth/lib sits inside it, and an
             ignoreFiles entry for the whole tree would swallow this app's own
             code too (verified — it turns the run into "No files analyzed"). -->
        <directory name="../../wa-system" />
        <directory name="../../wa-config" />
        <directory name="../../wa-plugins" />
        <ignoreFiles>
            <directory name="../../wa-system" />
            <directory name="../../wa-config" />
            <directory name="../../wa-plugins" />
        </ignoreFiles>
    </projectFiles>
</psalm>
